import { Directive } from './directive';
export declare const scanDOMTree: (root: Element, element: HTMLElement, localDirectives?: Directive<any>[]) => {
    paths: string[];
    update: (key: string) => void;
};
