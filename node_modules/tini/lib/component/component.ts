import { ComponentMixinInternals, ComponentMixin } from "../ComponentMixin";

export class Component extends ComponentMixin(HTMLElement) {
    constructor() {
        super();
    }

    protected requestUpdate() {
        requestAnimationFrame(() => {
            // @ts-ignore
            this[ComponentMixinInternals].update('*');
        });
    }
}